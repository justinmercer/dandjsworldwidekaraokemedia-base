# Tests Placeholder

This directory is reserved for future cross-service tests, smoke checks, and demo fixtures.

Wave 0A includes only the repo-wide smoke-test script in `scripts/smoke-test.ps1`. Future test fixtures must be demo-only and must not include real karaoke media, secrets, private URLs, or personal singer information.
