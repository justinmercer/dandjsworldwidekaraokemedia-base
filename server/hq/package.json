{
  "name": "@dandjs/hq-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Runnable HQ catalog foundation for operator-authorized karaoke metadata.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node --check src/catalogData.js && node --check src/catalogRepository.js && node --check src/httpServer.js && node --check src/index.js",
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
