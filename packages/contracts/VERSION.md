# Shared Contracts Version

Current foundation contract version: `v1`

The `v1` schemas include current HQ catalog and Wave 2A host sync-planning shapes plus planning contracts for later components. They do not imply that generated clients, the Windows host app, request web app, host downloading, local file transfer, playback, cleanup deletion execution, OBS, or Replay features are implemented.
