# Shared Contracts Version

Current foundation contract version: `v1`

The `v1` schemas are planning contracts for future components. They do not imply that the HQ server, Windows host app, request web app, synchronization, playback, OBS, or Replay features are implemented.
