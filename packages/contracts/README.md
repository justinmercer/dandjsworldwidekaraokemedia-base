# Shared Contracts Placeholder

This directory is reserved for future shared DTOs, JSON schemas, and versioned API contracts.

Wave 0A defines naming and feature-flag conventions only. It does not add server APIs, contract packages, generated clients, or compatibility code.
